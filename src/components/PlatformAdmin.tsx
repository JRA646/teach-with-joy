import { useEffect, useMemo, useState } from 'react'
import { Activity, GripVertical, LayoutDashboard, Menu, Plus, RefreshCw, Save, Shield, ToggleLeft, Workflow } from 'lucide-react'
import { supabase } from '../lib/supabase'
import { getDashboardWidgets, getFeatureFlags, getNavigationItems, saveFeatureFlag, upsertDashboardWidget, upsertNavigation } from '../services/platform'
import type { DashboardWidgetDefinition, NavigationItem } from '../types/platform'

type Tab = 'roles'|'navigation'|'widgets'|'features'|'audit'|'workflows'|'settings'
const tabs:{id:Tab;label:string;icon:any}[]=[{id:'roles',label:'Roles & permissions',icon:Shield},{id:'navigation',label:'Navigation',icon:Menu},{id:'widgets',label:'Dashboard widgets',icon:LayoutDashboard},{id:'features',label:'Feature flags',icon:ToggleLeft},{id:'audit',label:'Audit log',icon:Activity},{id:'workflows',label:'Workflows',icon:Workflow},{id:'settings',label:'Platform settings',icon:Save}]
export default function PlatformAdmin(){const[tab,setTab]=useState<Tab>('roles');return <div className="platform-admin"><div className="platform-admin-tabs">{tabs.map(({id,label,icon:Icon})=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}><Icon size={16}/>{label}</button>)}</div>{tab==='roles'&&<RolesPanel/>}{tab==='navigation'&&<NavigationPanel/>}{tab==='widgets'&&<WidgetsPanel/>}{tab==='features'&&<FeaturesPanel/>}{tab==='audit'&&<AuditPanel/>}{tab==='workflows'&&<WorkflowPanel/>}{tab==='settings'&&<SettingsPanel/>}</div>}
function NavigationPanel(){const[role,setRole]=useState('teacher'),[items,setItems]=useState<NavigationItem[]>([]),[loading,setLoading]=useState(false),[message,setMessage]=useState('');const load=async()=>{setLoading(true);setItems(await getNavigationItems(role));setLoading(false)};useEffect(()=>{void load()},[role]);const update=(i:number,p:Partial<NavigationItem>)=>setItems(c=>c.map((x,n)=>n===i?{...x,...p}:x));const move=(from:number,to:number)=>setItems(c=>{const n=[...c];const[item]=n.splice(from,1);n.splice(to,0,item);return n.map((x,i)=>({...x,sortOrder:i}))});const save=async()=>{setMessage('Saving…');const r=await Promise.all(items.map(item=>upsertNavigation({...item,role_key:role,item_key:item.key})));setMessage(r.every(x=>!x.error)?'Navigation saved.':'Some navigation items could not be saved.');await load()};return <section className="platform-panel"><PanelHead title="Navigation manager" text="Configure the menu per role without changing application code." refresh={load} loading={loading} action={<button className="platform-primary" onClick={save}><Save size={15}/> Save changes</button>}/><div className="platform-toolbar"><label>Role<select value={role} onChange={e=>setRole(e.target.value)}><option>teacher</option><option>student</option><option>admin</option></select></label></div><div className="platform-list">{items.map((item,i)=><div className="platform-list-row" key={item.key}><GripVertical size={17}/><div className="platform-grow"><input value={item.label} onChange={e=>update(i,{label:e.target.value})}/><small>{item.path} · {item.permission||'no permission'}</small></div><button disabled={i===0} onClick={()=>move(i,i-1)}>↑</button><button disabled={i===items.length-1} onClick={()=>move(i,i+1)}>↓</button><input className="platform-check" type="checkbox" checked={item.enabled} onChange={e=>update(i,{enabled:e.target.checked})}/></div>)}</div>{message&&<div className="platform-message">{message}</div>}</section>}
function WidgetsPanel(){const[role,setRole]=useState('teacher'),[widgets,setWidgets]=useState<DashboardWidgetDefinition[]>([]),[message,setMessage]=useState('');const load=async()=>setWidgets(await getDashboardWidgets(role));useEffect(()=>{void load()},[role]);const update=(i:number,p:Partial<DashboardWidgetDefinition>)=>setWidgets(c=>c.map((x,n)=>n===i?{...x,...p}:x));const save=async()=>{setMessage('Saving…');const r=await Promise.all(widgets.map(w=>upsertDashboardWidget({...w,role_key:role,widget_key:w.key})));setMessage(r.every(x=>!x.error)?'Widgets saved.':'Some widgets could not be saved.');await load()};return <section className="platform-panel"><PanelHead title="Dashboard widget manager" text="Control widget composition, order, visibility and feature gates." refresh={load} action={<button className="platform-primary" onClick={save}><Save size={15}/> Save changes</button>}/><div className="platform-toolbar"><label>Role<select value={role} onChange={e=>setRole(e.target.value)}><option>teacher</option><option>student</option></select></label></div><div className="platform-list">{widgets.map(widget=><div className="platform-list-row" key={widget.key}><GripVertical size={17}/><div className="platform-grow"><input value={widget.title} onChange={e=>update(widgets.indexOf(widget),{title:e.target.value})}/><small>{widget.component} · {widget.permission||'no permission'}</small></div><input type="number" min="0" value={widget.sortOrder} onChange={e=>update(widgets.indexOf(widget),{sortOrder:Number(e.target.value)})}/><input className="platform-check" type="checkbox" checked={widget.enabled} onChange={e=>update(widgets.indexOf(widget),{enabled:e.target.checked})}/></div>)}</div>{message&&<div className="platform-message">{message}</div>}</section>}
function FeaturesPanel(){const[flags,setFlags]=useState<Record<string,boolean>>({}),[message,setMessage]=useState('');const load=async()=>setFlags(await getFeatureFlags());useEffect(()=>{void load()},[]);const save=async(key:string,value:boolean)=>{setFlags(x=>({...x,[key]:value}));const r=await saveFeatureFlag(key,value);setMessage(r.error?`Unable to save ${key}.`:`${key} updated.`)};return <section className="platform-panel"><PanelHead title="Feature flags" text="Safely enable platform capabilities and stage future rollouts." refresh={load}/><div className="platform-flag-grid">{Object.entries(flags).map(([key,enabled])=><div className="platform-flag" key={key}><div><strong>{key}</strong><small>Runtime platform flag</small></div><button className={enabled?'platform-toggle on':'platform-toggle'} onClick={()=>void save(key,!enabled)}>{enabled?'Enabled':'Disabled'}</button></div>)}</div>{message&&<div className="platform-message">{message}</div>}</section>}
function RolesPanel(){const[roles,setRoles]=useState<any[]>([]),[permissions,setPermissions]=useState<any[]>([]),[selected,setSelected]=useState(''),[rolePermissionKeys,setRolePermissionKeys]=useState<Set<string>>(new Set());const load=async()=>{const[{data:r},{data:p}]=await Promise.all([supabase.from('platform_roles').select('*').order('label'),supabase.from('platform_permissions').select('*').order('key')]);setRoles(r||[]);setPermissions(p||[]);if(!selected&&r?.[0])setSelected(r[0].key)};useEffect(()=>{void load()},[]);useEffect(()=>{if(selected)void(async()=>{const{data}=await supabase.from('platform_role_permissions').select('permission_key').eq('role_key',selected);setRolePermissionKeys(new Set((data||[]).map(x=>x.permission_key)))})()},[selected]);const role=roles.find(r=>r.key===selected),grouped=useMemo(()=>permissions.reduce((a,p)=>{(a[p.key?.split('.')[0]||'general']||=[]).push(p);return a},{} as Record<string,any[]>),[permissions]);const toggle=async(permissionKey:string,enabled:boolean)=>{if(!role)return;if(enabled)await supabase.from('platform_role_permissions').upsert({role_key:role.key,permission_key:permissionKey});else await supabase.from('platform_role_permissions').delete().eq('role_key',role.key).eq('permission_key',permissionKey);setRolePermissionKeys(c=>{const n=new Set(c);enabled?n.add(permissionKey):n.delete(permissionKey);return n})};return <section className="platform-panel"><PanelHead title="Roles & permissions" text="Create extensible roles and assign least-privilege capabilities." refresh={load} action={<button className="platform-primary" onClick={async()=>{const key=prompt('Role key (e.g. coordinator)')?.trim().toLowerCase().replace(/\s+/g,'_');if(!key)return;await supabase.from('platform_roles').insert({key,label:key.replaceAll('_',' '),is_system:false});await load()}}><Plus size={15}/> Add role</button>}/><div className="platform-role-layout"><div className="platform-role-list">{roles.map(r=><button className={selected===r.key?'active':''} key={r.key} onClick={()=>setSelected(r.key)}><strong>{r.label}</strong><small>{r.key}</small></button>)}</div><div className="platform-permissions">{role?Object.entries(grouped).map(([module,list])=><div key={module}><h3>{module}</h3>{(list as any[]).map(p=><label className="permission-row" key={p.key}><input type="checkbox" checked={rolePermissionKeys.has(p.key)} onChange={e=>void toggle(p.key,e.target.checked)}/><span><strong>{p.label}</strong><small>{p.key}</small></span></label>)}</div>):<p>Select a role.</p>}</div></div></section>}
function AuditPanel(){const[rows,setRows]=useState<any[]>([]);const load=async()=>{const{data}=await supabase.from('platform_activity_log').select('id,action,entity_type,entity_id,metadata,created_at,actor_id').order('created_at',{ascending:false}).limit(100);setRows(data||[])};useEffect(()=>{void load()},[]);return <section className="platform-panel"><PanelHead title="Audit log" text="Review platform activity and configuration changes." refresh={load}/><div className="platform-audit">{rows.map(r=><div key={r.id}><div><strong>{r.action}</strong><small>{r.entity_type||'platform'} · {r.actor_id||'system'}</small></div><time>{new Date(r.created_at).toLocaleString()}</time><code>{JSON.stringify(r.metadata||{})}</code></div>)}</div></section>}
function WorkflowPanel(){const[rows,setRows]=useState<any[]>([]);const load=async()=>{const{data}=await supabase.from('platform_workflows').select('*').order('created_at',{ascending:false});setRows(data||[])};useEffect(()=>{void load()},[]);return <section className="platform-panel"><PanelHead title="Workflow engine" text="Define trigger → condition → action automations." refresh={load} action={<button className="platform-primary" onClick={async()=>{await supabase.from('platform_workflows').insert({name:'New workflow',trigger_type:'event',definition:{conditions:[],actions:[]}});await load()}}><Plus size={15}/> New workflow</button>}/><div className="platform-workflow-grid">{rows.map(r=><div className="platform-workflow-card" key={r.id}><strong>{r.name}</strong><small>{r.trigger_type}</small><button onClick={async()=>{await supabase.from('platform_workflows').update({enabled:!r.enabled}).eq('id',r.id);await load()}}>{r.enabled?'Enabled':'Disabled'}</button></div>)}</div></section>}
function SettingsPanel(){const[settings,setSettings]=useState(()=>{try{return JSON.parse(localStorage.getItem('teachwithjoy-platform-settings')||'{}')}catch{return {}}});const save=()=>{localStorage.setItem('teachwithjoy-platform-settings',JSON.stringify(settings));void supabase.from('app_settings').upsert({key:'platform',value:{name:settings.name||'TeachWithJoy',default_timezone:settings.timezone||'Asia/Manila'},is_public:true});alert('Platform settings saved.')};return <section className="platform-panel"><PanelHead title="Platform settings" text="Manage core platform defaults and preferences."/><div className="platform-settings-form"><label>Platform name<input value={settings.name||'TeachWithJoy'} onChange={e=>setSettings({...settings,name:e.target.value})}/></label><label>Default timezone<input value={settings.timezone||'Asia/Manila'} onChange={e=>setSettings({...settings,timezone:e.target.value})}/></label><label>Command palette<input type="checkbox" checked={settings.commandPalette!==false} onChange={e=>setSettings({...settings,commandPalette:e.target.checked})}/></label><button className="platform-primary" onClick={save}><Save size={15}/> Save settings</button></div></section>}
function PanelHead({title,text,refresh,loading,action}:{title:string;text:string;refresh?:()=>void;loading?:boolean;action?:React.ReactNode}){return <div className="platform-panel-head"><div><span className="platform-eyebrow">PLATFORM CONTROL</span><h2>{title}</h2><p>{text}</p></div><div className="platform-actions">{refresh&&<button onClick={refresh} disabled={loading}><RefreshCw size={15}/> Refresh</button>}{action}</div></div>}
