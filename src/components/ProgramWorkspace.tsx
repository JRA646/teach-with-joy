import { useState } from 'react'

export default function ProgramWorkspace() {
  const [tab, setTab] = useState('overview')
  const sessions = Array.from({ length: 20 }, (_, i) => ({ number: i + 1, status: i < 3 ? 'Completed' : 'Needs scheduling' }))
  return <div className="page"><div className="page-title"><div><span className="eyebrow">ENROLLMENT</span><h1>My learning program</h1><p>20 sessions · attendance, make-ups and contract benefits</p></div></div><div className="stat-grid"><div className="stat-card"><div><strong>20</strong><small>Total sessions</small></div></div><div className="stat-card"><div><strong>3</strong><small>Completed</small></div></div><div className="stat-card"><div><strong>17</strong><small>Remaining</small></div></div><div className="stat-card"><div><strong>3</strong><small>Postponements</small></div></div></div><section className="panel"><div className="panel-head"><div><h3>Program benefits</h3><p className="panel-subtitle">The application now models enrollment instead of treating every lesson as a separate purchase.</p></div></div><div className="program-benefits"><div><strong>20</strong><span>sessions per month</span></div><div><strong>3</strong><span>postponements on 3-month contract</span></div><div><strong>2</strong><span>free e-books on 3-month contract</span></div><div><strong>₱</strong><span>upfront enrollment payment</span></div></div></section><section className="panel"><div className="panel-head"><div><h3>Session ledger</h3><p className="panel-subtitle">Student absence consumes a session. Teacher absence creates a make-up. Holidays do not consume a session.</p></div><div><button className={tab === 'overview' ? 'btn primary small' : 'btn small'} onClick={() => setTab('overview')}>All</button> <button className={tab === 'attendance' ? 'btn primary small' : 'btn small'} onClick={() => setTab('attendance')}>Attendance</button></div></div><div className="session-ledger">{sessions.filter(s => tab === 'overview' || s.status === 'Completed').map(s => <div className="session-row" key={s.number}><div className="session-number">{s.number}</div><div className="session-main"><strong>Session {s.number}</strong><span>{s.status === 'Completed' ? 'Lesson completed' : 'Not scheduled yet'}</span></div><span className="session-status">{s.status}</span></div>)}</div></section></div>
}
