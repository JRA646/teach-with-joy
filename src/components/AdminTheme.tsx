import {Save} from 'lucide-react'
type Props={theme:any;setTheme:React.Dispatch<React.SetStateAction<any>>;save:()=>void}
const colors=[['primary_color','Primary'],['primary_dark','Primary dark'],['accent_color','Accent'],['background_color','Background'],['surface_color','Surface'],['text_color','Muted text background'],['muted_color','Muted text']]
const update=(setTheme:Props['setTheme'],key:string,value:string)=>setTheme((x:any)=>({...x,[key]:value}))
export default function AdminTheme({theme,setTheme,save}:Props){return <section className="admin-panel"><div className="admin-panel-heading"><div><span className="admin-kicker">BRANDING & THEME</span><h3>Customize your site</h3><p>Update the brand, footer and color system. Changes are saved to Supabase and reflected on the public site.</p></div><button className="admin-btn primary" onClick={save}><Save size={14}/> Save theme</button></div><div className="admin-form-grid"><label className="admin-field"><strong>Site name</strong><input value={theme.site_name||'TeachWithJoy'} onChange={e=>update(setTheme,'site_name',e.target.value)}/></label><label className="admin-field"><strong>Footer text</strong><input value={theme.footer_text||''} onChange={e=>update(setTheme,'footer_text',e.target.value)}/></label><label className="admin-field"><strong>Copyright text</strong><input value={theme.copyright_text||''} onChange={e=>update(setTheme,'copyright_text',e.target.value)}/></label></div><div className="theme-layout"><div className="theme-fields">{colors.map(([k,l])=><label className="theme-field" key={k}><strong>{l}</strong><div className="color-input"><input type="color" value={theme[k]||'#ffffff'} onChange={e=>update(setTheme,k,e.target.value)}/><input value={theme[k]||''} onChange={e=>update(setTheme,k,e.target.value)}/></div></label>)}</div><div className="theme-preview" style={{background:theme.background_color||'#f7f6fb',color:theme.text_color||'#111827'}}><small style={{color:theme.primary_color}}>LIVE PREVIEW</small><div className="preview-nav" style={{background:theme.surface_color}}><b>{theme.site_name||'TeachWithJoy'}</b><button style={{background:theme.primary_color,color:'#fff'}}>Get Started</button></div><h4>Learn smarter,<br/><span style={{color:theme.primary_color}}>achieve more.</span></h4><p style={{color:theme.muted_color}}>Your branding changes appear here.</p><button style={{background:theme.primary_color,color:'#fff'}}>Book a Session</button></div></div></section>}
