export type WorkflowCondition = { field: string; operator: 'equals'|'not_equals'|'contains'|'greater_than'|'less_than'|'exists'; value?: unknown }
export function readPath(input: Record<string, unknown>, path: string): unknown { return path.split('.').reduce<unknown>((value, key) => value && typeof value === 'object' ? (value as Record<string, unknown>)[key] : undefined, input) }
export function matchesConditions(conditions: WorkflowCondition[] = [], payload: Record<string, unknown>) { return conditions.every(condition => { const actual=readPath(payload,condition.field); switch(condition.operator){case'exists':return actual!==undefined&&actual!==null;case'equals':return actual===condition.value;case'not_equals':return actual!==condition.value;case'contains':return String(actual??'').toLowerCase().includes(String(condition.value??'').toLowerCase());case'greater_than':return Number(actual)>Number(condition.value);case'less_than':return Number(actual)<Number(condition.value)}}) }
